cmake_minimum_required(VERSION 3.24)
project(OpenGodAudioLoopback LANGUAGES CXX)
if(NOT WIN32 OR NOT MSVC)
  message(FATAL_ERROR "This verifier requires Windows and the Microsoft Windows SDK")
endif()
add_executable(opengod-audio-loopback main.cpp)
target_compile_features(opengod-audio-loopback PRIVATE cxx_std_20)
target_compile_definitions(opengod-audio-loopback PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
target_compile_options(opengod-audio-loopback PRIVATE /W4 /WX /utf-8)
target_link_libraries(opengod-audio-loopback PRIVATE mmdevapi ole32)
include(CTest)
add_test(NAME audio_spectrum_self_test COMMAND opengod-audio-loopback --self-test)
