add_library(opengod_core STATIC browser_state.cc navigation.cc cve_feed.cc)
target_include_directories(opengod_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/..")
target_compile_features(opengod_core PUBLIC cxx_std_20)
if(MSVC)
  target_compile_options(opengod_core PRIVATE /W4 /permissive-)
else()
  target_compile_options(opengod_core PRIVATE -Wall -Wextra -Wpedantic)
endif()

if(BUILD_TESTING)
  add_executable(opengod_core_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/core/browser_state_test.cc")
  target_link_libraries(opengod_core_tests PRIVATE opengod_core)
  add_test(NAME opengod_core_tests COMMAND opengod_core_tests)
endif()

if(BUILD_TESTING)
 add_executable(opengod_cve_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/core/cve_feed_test.cc")
 target_link_libraries(opengod_cve_tests PRIVATE opengod_core)
 add_test(NAME opengod_cve_tests COMMAND opengod_cve_tests)
endif()
