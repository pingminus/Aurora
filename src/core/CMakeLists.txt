add_library(aurora_core STATIC browser_state.cc navigation.cc cve_feed.cc)
target_include_directories(aurora_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/..")
target_compile_features(aurora_core PUBLIC cxx_std_20)
if(MSVC)
  target_compile_options(aurora_core PRIVATE /W4 /permissive-)
else()
  target_compile_options(aurora_core PRIVATE -Wall -Wextra -Wpedantic)
endif()

if(BUILD_TESTING)
  add_executable(aurora_core_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/core/browser_state_test.cc")
  target_link_libraries(aurora_core_tests PRIVATE aurora_core)
  add_test(NAME aurora_core_tests COMMAND aurora_core_tests)
endif()

if(BUILD_TESTING)
 add_executable(aurora_cve_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/core/cve_feed_test.cc")
 target_link_libraries(aurora_cve_tests PRIVATE aurora_core)
 add_test(NAME aurora_cve_tests COMMAND aurora_cve_tests)
endif()
