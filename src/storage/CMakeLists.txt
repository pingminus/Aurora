add_library(aurora_storage STATIC session_store.cc)
target_link_libraries(aurora_storage PUBLIC aurora_core)
target_compile_features(aurora_storage PUBLIC cxx_std_20)
if(MSVC)
  target_compile_options(aurora_storage PRIVATE /W4 /permissive-)
else()
  target_compile_options(aurora_storage PRIVATE -Wall -Wextra -Wpedantic)
endif()
if(BUILD_TESTING)
  add_executable(aurora_storage_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/storage/session_store_test.cc")
  target_link_libraries(aurora_storage_tests PRIVATE aurora_storage)
  add_test(NAME aurora_storage_tests COMMAND aurora_storage_tests)
endif()
