add_library(opengod_storage STATIC session_store.cc)
target_link_libraries(opengod_storage PUBLIC opengod_core)
target_compile_features(opengod_storage PUBLIC cxx_std_20)
if(MSVC)
  target_compile_options(opengod_storage PRIVATE /W4 /permissive-)
else()
  target_compile_options(opengod_storage PRIVATE -Wall -Wextra -Wpedantic)
endif()
if(BUILD_TESTING)
  add_executable(opengod_storage_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../tests/storage/session_store_test.cc")
  target_link_libraries(opengod_storage_tests PRIVATE opengod_storage)
  add_test(NAME opengod_storage_tests COMMAND opengod_storage_tests)
endif()
