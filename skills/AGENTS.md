# Project skill scope

Keep each playbook concise, with YAML `name` and `description`, a concrete trigger, OPENGOD-specific invariants and relevant checks. Reference shared docs instead of duplicating the constitution. Skills support the user's task and do not create permission to publish, install or mutate unrelated systems.
