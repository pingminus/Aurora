# Role documentation scope

Each role has a bounded ownership area and named handoffs. Roles coordinate rather than duplicate implementation. Keep ownership guidance aligned with actual modules as the repository grows. A role document does not imply that its planned subsystem already exists.
