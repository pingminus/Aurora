# Documentation scope

Separate intended architecture from implemented and verified behavior. Build commands must match actual targets and scripts. Record native compilation and runtime evidence separately. Update the roadmap when features gain real behavior; never infer completeness from a directory or interface. Link authoritative dependency references without copying upstream manuals.
